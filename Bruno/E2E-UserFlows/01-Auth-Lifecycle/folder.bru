meta {
  name: 01-Auth-Lifecycle
}

docs {
  Flow: Registrer → Login → Get Me → Refresh token → Logout.
  Sætter: testUsername, testEmail, testPassword, createdUserId, authToken, refreshToken (bruges i senere flows).
}
