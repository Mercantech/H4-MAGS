meta {
  name: Generisk OAuth login - Virker med alle providers (Google, Microsoft, GitHub, etc.)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/auth/oauth-login
  body: json
  auth: none
}

body:json {
  {
    "provider": "",
    "accessToken": ""
  }
}
